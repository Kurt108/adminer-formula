{% macro sls_block(dict) %}
    {% for key, value in dict.items() %}
    - {{ key }}: {{ value|json() }}
    {% endfor %}
{% endmacro %}

{% set adminer = salt['pillar.get']('adminer', {
    'connections': {
        'managed': {},
    },
}, merge=True) %}
